---
phase: XSB-06-navigation-ia
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [NAV-01]
files_modified:
  - miniprogram/app.json
  - miniprogram/pages/income/index.js
  - scripts/generate-tab-icons.js
autonomous: true
must_haves:
  truths:
    - tabBar lists exactly home and profile
    - record and income remain in pages array but not in tabBar
    - income calendar daytap uses navigateTo to record
  artifacts:
    - path: miniprogram/app.json
      provides: 2-tab configuration
  key_links:
    - from: miniprogram/pages/income/index.js
      to: /pages/record/index
      via: wx.navigateTo
---

# Plan 06-01: TabBar Config & Routing Foundation

**Goal:** Demote record/income from tabBar; fix the only switchTab call site; trim tab icon script to 2 pairs.

<objective>
Wave 1 — structural routing. No UI changes yet except app.json tab list.
Output: compilable mini program with 2 tabs; income→record navigation uses navigateTo.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/XSB-06-navigation-ia/06-CONTEXT.md
@.planning/phases/XSB-06-navigation-ia/06-RESEARCH.md
@miniprogram/app.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: app.json 2-tab configuration</name>
  <action>
    Edit miniprogram/app.json:
    - tabBar.list: keep only home + profile entries; remove record and income
    - pages array: add "pages/settings/index" after profile (placeholder ok if page not created yet — create stub in 06-02 or minimal empty page here to avoid compile error)
    - Ensure record and income remain in pages[] (not tabBar)

    Run DevTools compile check mentally: tabBar pagePath must exist in pages[].
  </action>
  <verify>
    grep -c 'pagePath' miniprogram/app.json tabBar section equals 2
    grep 'pages/settings' miniprogram/app.json
  </verify>
</task>

<task type="auto">
  <name>Task 2: Fix income → record navigation</name>
  <action>
    In miniprogram/pages/income/index.js onCalendarDayTap:
    Replace wx.switchTab({ url: '/pages/record/index' }) with wx.navigateTo({ url: '/pages/record/index' }).
    Keep globalData.editRecordDate assignment unchanged.
  </action>
  <verify>
    grep navigateTo miniprogram/pages/income/index.js
    ! grep switchTab miniprogram/pages/income/index.js | grep record
  </verify>
</task>

<task type="auto">
  <name>Task 3: Tab icon script trim</name>
  <action>
    Update scripts/generate-tab-icons.js to generate only tab-home and tab-profile pairs.
    Run node scripts/generate-tab-icons.js to refresh PNGs in miniprogram/assets/icons/.
  </action>
  <verify>
    ls miniprogram/assets/icons/tab-home*.png miniprogram/assets/icons/tab-profile*.png
  </verify>
</task>

</tasks>

<verification>
```bash
npm run test:core
grep -A20 '"tabBar"' miniprogram/app.json
```
</verification>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | app.json routes | mitigate | No auth routes changed; grep all switchTab after edit |
</threat_model>
