---
phase: XSB-06-navigation-ia
plan: 02
type: execute
wave: 2
depends_on: [06-01]
requirements: [NAV-01]
files_modified:
  - miniprogram/pages/settings/index.js
  - miniprogram/pages/settings/index.wxml
  - miniprogram/pages/settings/index.wxss
  - miniprogram/pages/settings/index.json
autonomous: true
must_haves:
  truths:
    - settings page contains all salary/insurance/work/cloud/save logic formerly on profile
    - saveSettings + cloud sync enable/disable/logout work identically to Phase 5 profile
    - onSave triggers syncNow + refresh when cloud enabled
  artifacts:
    - path: miniprogram/pages/settings/index.js
      provides: settings CRUD + cloud sync handlers
---

# Plan 06-02: Settings Sub-Page Extraction

**Goal:** Create `pages/settings` by migrating form and cloud-sync UI from profile.

<objective>
Wave 2 — move heavy scroll content off tab profile. Profile will be slimmed in 06-03.
Output: fully functional settings sub-page (may lack sub-nav until 06-04).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/XSB-06-navigation-ia/06-CONTEXT.md
@.planning/phases/XSB-06-navigation-ia/06-PATTERNS.md
@miniprogram/pages/profile/index.js
@miniprogram/pages/profile/index.wxml
@miniprogram/pages/profile/index.wxss
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold settings page</name>
  <action>
    Create miniprogram/pages/settings/index.json — navigationStyle custom, backgroundColor #0a101f.
    Create index.wxss — @import or rely on app.wxss ui.wxss (match profile).
  </action>
</task>

<task type="auto">
  <name>Task 2: Migrate WXML form blocks</name>
  <action>
    Copy from profile/index.wxml to settings/index.wxml:
    - All cards from 税前月薪 through 云端备份 and 保存设置 button
    - Use page-shell (sub padding class if 06-04 not done yet use page-shell without tab)
    - Do NOT copy preview-card (stays on profile)
    - Include pageReady block pattern for deferred insurance/work sections
  </action>
</task>

<task type="auto">
  <name>Task 3: Migrate JS handlers</name>
  <action>
    Copy from profile/index.js to settings/index.js:
    - data fields for forms, presets, cloudSync, canSave, pageReady
    - onLoad, onShow (refreshCloudSyncUI), updatePreview, onSave (with syncNow fix)
    - onToggleCloudSync, onLogoutCloud, all preset/slider handlers
    - behaviors: safe-area
    Remove duplicated logic from profile in plan 06-03 — for this plan, profile may temporarily duplicate until 06-03 cleans it
  </action>
  <verify>
    grep onSave miniprogram/pages/settings/index.js
    grep enableCloudSync miniprogram/pages/settings/index.js
  </verify>
</task>

</tasks>

<verification>
```bash
npm run test:core
grep -q onToggleCloudSync miniprogram/pages/settings/index.js
```
Manual: navigate to /pages/settings/index in DevTools — form renders, save works.
</verification>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Tampering | settings save | mitigate | Reuse saveSettings service unchanged; no new storage keys |
</threat_model>
