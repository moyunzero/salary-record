---
phase: XSB-06-navigation-ia
plan: 03
type: execute
wave: 3
depends_on: [06-02]
requirements: [NAV-01]
files_modified:
  - miniprogram/pages/profile/index.js
  - miniprogram/pages/profile/index.wxml
  - miniprogram/pages/profile/index.wxss
autonomous: true
must_haves:
  truths:
    - profile shows preview card + 4 hub link rows only (no inline salary forms)
    - hub displays cloud backup summary read-only
    - all four navigators reach correct sub-pages
    - onShow refreshes preview hourly and cloud summary
  artifacts:
    - path: miniprogram/pages/profile/index.wxml
      provides: hub layout ~1 screen
---

# Plan 06-03: Profile Hub Refactor

**Goal:** Slim profile tab to thin hub per 06-CONTEXT.md.

<objective>
Wave 3 — remove migrated form code from profile; add link rows and preview-only refresh.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/XSB-06-navigation-ia/06-CONTEXT.md
@.planning/phases/XSB-06-navigation-ia/06-PATTERNS.md
@miniprogram/pages/profile/index.js
@miniprogram/pages/settings/index.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hub WXML</name>
  <action>
    Replace profile/index.wxml body (after preview-card) with single card containing 4 profile-link-row items:
    1. 工时记录 — hint 补录或修改历史工时
    2. 收入趋势 — hint 周/月统计与日历
    3. 工作设置 — hint 月薪、五险一金与工时
    4. 云端备份 — dynamic: {{cloudSyncEnabled ? '已开启 · ' + lastSyncDisplay : '未开启'}} 
    Remove all salary/insurance/work/cloud form blocks and save button.
  </action>
</task>

<task type="auto">
  <name>Task 2: Hub JS</name>
  <action>
    Remove from profile/index.js: form state, onSave, sync toggles, preset handlers (now in settings).
    Keep: preview fields, updatePreview, refreshCloudSyncUI (summary), onShow/onLoad.
    Add: onGoRecord, onGoIncome, onGoSettings — navigateTo respective paths.
    Cloud row tap → onGoSettings.
    onShow: call updatePreview() + refreshCloudSyncUI().
  </action>
</task>

<task type="auto">
  <name>Task 3: Hub styles</name>
  <action>
    Add to ui.wxss (or profile wxss if minimal): .profile-link-row, .profile-link-title, .profile-link-hint, .profile-link-chevron — match sync-logout spacing.
  </action>
</task>

</tasks>

<verification>
```bash
! grep onSave miniprogram/pages/profile/index.js
grep onGoRecord miniprogram/pages/profile/index.js
wc -l miniprogram/pages/profile/index.wxml  # should be significantly shorter
```
</verification>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Denial of service | hub onShow | accept | Lightweight getSettings only |
</threat_model>
