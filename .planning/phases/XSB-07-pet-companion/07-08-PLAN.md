---
phase: XSB-07-pet-companion
plan: 08
type: execute
wave: 6
depends_on: [07-07]
requirements: [COMP-01]
files_modified:
  - miniprogram/components/cat-pet/index.js
  - miniprogram/components/cat-pet/index.json
  - miniprogram/pages/home/index.wxml
  - miniprogram/core/pet-arcs.js
autonomous: true
must_haves:
  truths:
    - useMicroFsm true by default replaces pickArc main loop with pickNextMicroBehavior (D-M3)
    - pet-interrupt.js sleep tap / wake chain unchanged and still wired
    - 30s/3 tap recordTap integrated in onTap; excited patrol visible via walk surge (D-M5)
    - done-night one-shot yawn→sleep3→sleep4 entry then hold sleep4 until interrupt (D-M4)
    - Debug panel shows micro state; 11 scene presets exercise micro routing
    - useArcFsm false still falls back to CONTEXT_CLIP_MAP for debug rollback
  artifacts:
    - path: miniprogram/components/cat-pet/index.js
      provides: microRunner replacing arc stepper
      contains: useMicroFsm
    - path: miniprogram/pages/home/index.wxml
      provides: debug lines for microFsm / excitedPatrol
  key_links:
    - from: miniprogram/components/cat-pet/index.js
      to: miniprogram/core/pet-micro.js
      via: pickNextMicroBehavior on block complete
      pattern: pickNextMicroBehavior
    - from: miniprogram/components/cat-pet/index.js
      to: miniprogram/core/pet-interrupt.js
      via: onArcTap / sleep interrupt preserved
      pattern: pickSleepInterrupt|postInterruptResume
---

# Plan 07-08: cat-pet Micro Runner Integration (Wave 6)

**Goal:** Replace arc FSM main path in cat-pet with micro block runner; wire tap tracker and debug for all 11 scene keys per D-M3/D-M5.

<objective>
Wave 6 runtime — microRunner in cat-pet consumes pet-micro.js; deprecate pickArc step loop while retaining pet-interrupt and sleep helpers.

Purpose: User-visible COMP-01 quality — cat behaves in 1–3 block bursts with more walk, not full arc cycles.
Output: cat-pet/index.js micro FSM default on; arc path gated behind useArcFsm rollback flag.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/XSB-07-pet-companion/07-MICRO-CONTEXT.md
@docs/PET-MICRO-BEHAVIOR.md
@miniprogram/core/pet-micro.js
@miniprogram/components/cat-pet/index.js
@miniprogram/core/pet-interrupt.js
@miniprogram/core/pet-arcs.js
@miniprogram/pages/home/index.js
@miniprogram/pages/home/index.wxml
</context>

<artifacts_this_phase_produces>
- Property `useMicroFsm: { type: Boolean, value: true }` on cat-pet; when true and not forceClip, micro path active
- `useArcFsm` default becomes false when useMicroFsm true (mutually exclusive; micro takes precedence)
- `_microSession = createMicroSession()` on attach; reset on scene/escalation change
- `_runMicroBlock(block)` — dispatches clip / clipHalf / walk / microWalk / hold like applyArcStep did for kinds
- `_scheduleNextMicro()` — calls pickNextMicroBehavior after block complete; 35% chain handled via session.microChainRemaining
- `_onMicroBlockComplete()` — onBlockComplete + schedule next unless hold or interrupt
- `recordTap` in onTap/onArcTap path before interrupt handling; skip double-tap wash dedupe from tap count
- done-night: `_runDoneNightEntry()` one-shot yawn_sit → sleep3 → sleep4 then inSleep4Hold for pickNextMicroBehavior
- overtime L3/L4 stress via micro weights + clipHalf alternation (remove stress/meltdown arc dependency for micro path)
- Remove ambient walk scheduler when useMicroFsm (walk comes from micro pool per D-M2)
- pet-arcs.js: mark pickArc/ARC_DEFS step path @deprecated in comment; keep resolveDoneBand, sceneArcKey, sleepSide exports
- Debug payload: useMicroFsm, excitedPatrol, microBlockKind, interestTarget, doneBand; home wxml lines for micro state
- PET_DEBUG_SCENARIOS: split done into done-active (19:00) and done-night (23:00) presets — 12 chips covering 11 routing keys (overtime L1–4 separate)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Micro session + block runner replace arc stepper (D-M3)</name>
  <read_first>
    - miniprogram/components/cat-pet/index.js — resetArcState, applyArcStep, advanceArcStep, startLoop, pickWanderTarget, updateMovement
    - miniprogram/core/pet-micro.js — pickNextMicroBehavior, resolveWalkTarget, onBlockComplete
    - docs/PET-MICRO-BEHAVIOR.md §2 block kinds, §9 cat-pet改动要点 items 1–2
  </read_first>
  <action>
    Add useMicroFsm property default true. In attached()/scene observer: if useMicroFsm, call resetMicroState() instead of resetArcState().

    resetMicroState: createMicroSession(); clear arc fields; pick first block via pickNextMicroBehavior with resolveDoneBand for done scene; call _runMicroBlock.

    Implement _runMicroBlock(block):
    - kind clip: set _activeClipName, loop per clip def, optional singleCycle flag
    - kind clipHalf: play half cycle then complete
    - kind walk: set _arcWalkRemaining from block.walkSteps, resolve target tx/ty from block.target or resolveWalkTarget, trigger walk via existing pickWanderTarget/triggerArcWalk path
    - kind microWalk: 1 step wander with 3–6s delay chain flag on session
    - kind hold: set sleep4 clip, _microHold=true, no auto schedule until interrupt

    _scheduleNextMicro: if _microHold or _interruptChain active, return; else pickNextMicroBehavior with inSleep4Hold flag when done-night sleep4; _runMicroBlock result.

    Hook block completion in tickFrame/advanceArcStep equivalent: when clip cycle completes or walk steps done, call _onMicroBlockComplete → onBlockComplete(session, block, {tx,ty}).

    When useMicroFsm: skip maybeAmbientWalk, scheduleAmbientWalk, pickNextArc, applyArcStep main path. Guard all useArcFsm branches: useMicroFsm takes precedence.

    Keep useArcFsm path intact behind false useMicroFsm for rollback (do not delete arc code yet).

    Scene/escalation property observer: reset micro session and re-pick behavior; preserve excitedPatrolUntil wall clock across scene switch per spec §3.
  </action>
  <verify>
    <automated>npm run test:core && grep -q useMicroFsm miniprogram/components/cat-pet/index.js && grep -q pickNextMicroBehavior miniprogram/components/cat-pet/index.js</automated>
  </verify>
  <acceptance_criteria>
    - useMicroFsm default true
    - pickNextMicroBehavior drives block transitions
    - Arc stepper not invoked when useMicroFsm true
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Tap tracker + done-night entry + interrupt preservation (D-M4, D-M5)</name>
  <read_first>
    - miniprogram/core/pet-interrupt.js — onArcTap flow, pickSleepInterrupt, postInterruptResume
    - miniprogram/components/cat-pet/index.js — onArcTap, onCatTap, DOUBLE_TAP_WASH dedupe
    - docs/PET-MICRO-BEHAVIOR.md §3 tap trigger, §4 done-night hold, §9 items 3–5
  </read_first>
  <action>
    In onArcTap/onTap when useMicroFsm: call recordTap(session, now) on valid zone taps (head/body/tail); exclude double-tap wash from tapTimes push.

    If recordTap returns triggeredPatrol: set excited state (already in session); optionally immediate _scheduleNextMicro with excited flag; defer meow_mid sfx to 07-09 (no-op playCompanionSfx call ok).

    Preserve full pet-interrupt sleep wake chain: sleep tier clips still route through pickSleepInterrupt; after interrupt postInterruptResume feeds back into _scheduleNextMicro not pickArc.

    done-night entry sequence on first enter done-night band: run fixed micro sequence yawn_sit (singleCycle) → sleep3_* → sleep4_* then set inSleep4Hold; subsequent pickNextMicroBehavior returns hold only until interrupt (D-M4).

    overtime L3/L4: rely on pet-micro gates; implement clipHalf alternation for hiss/paw in _runMicroBlock when block specifies stressPhase if needed.

    Remove dependency on _arcId stress/meltdown for micro path.

    canCompanionNudgeDuringArc: treat useMicroFsm like arc for nudge eligibility (pass useMicroFsm to evaluateCompanionNudge or alias useArcFsm in call).
  </action>
  <verify>
    <automated>npm run test:core && grep -q recordTap miniprogram/components/cat-pet/index.js && grep -q inSleep4Hold miniprogram/components/cat-pet/index.js</automated>
  </verify>
  <acceptance_criteria>
    - recordTap wired on tap path
    - done-night sleep4 hold enforced
    - pet-interrupt sleep flows still callable
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Debug panel micro fields + 11-scene presets (Claude discretion)</name>
  <read_first>
    - miniprogram/pages/home/index.js — PET_DEBUG_SCENARIOS, onPetDebugScenario
    - miniprogram/pages/home/index.wxml — pet-debug-line arc display
    - miniprogram/components/cat-pet/index.js — emitDebugInfo payload
  </read_first>
  <action>
    Extend cat-pet debug payload: useMicroFsm, excitedPatrol (isExcitedPatrol), microBlockKind, interestId, tapCount30s, doneBand.

    home/index.wxml: add debug lines when petDebugInfo.useMicroFsm — show 兴奋巡视 / micro block kind / interest target; keep arc lines when useArcFsm fallback only.

    Extend PET_DEBUG_SCENARIOS to 12 entries: add doneActive (done @19:00) and doneNight (done @23:00) replacing single done chip OR add sub-band override field consumed by cat-pet done band resolver in debug mode.

    Ensure all 11 micro routing keys reachable: beforeWork, offDuty, onShift, lunch, dinner, nightShift, overtime-L1–L4, done-active, done-night.

    Add pet-debug button「触发兴奋巡视」optional: inject 3 tapTimes into session via cat-pet method exposed for debug only.

    Do NOT change production home layout outside debug panel.
  </action>
  <verify>
    <automated>npm run test:core && grep -q useMicroFsm miniprogram/pages/home/index.wxml && node -e "const s=require('./miniprogram/pages/home/index.js'); process.exit(0)" 2>/dev/null || grep -c doneNight miniprogram/pages/home/index.js | awk '{exit ($1>=1)?0:1}'</automated>
  </verify>
  <acceptance_criteria>
    - Debug shows micro state when useMicroFsm
    - done-active and done-night separately presettable
    - All overtime L1–4 presets unchanged and working with micro runner
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component tap input | User touch → recordTap / interrupt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08-01 | Denial of Service | Micro rescheduling loop | mitigate | hold + interrupt gates stop _scheduleNextMicro |
| T-07-08-02 | Tampering | Debug patrol inject | accept | Debug-only method; gated by petDebugEnabled |
</threat_model>

<verification>
```bash
npm run test:core
grep -E 'useMicroFsm|pickNextMicroBehavior|recordTap' miniprogram/components/cat-pet/index.js
```
DevTools: enable pet debug → cycle 11 scene presets → cat animates without arcId stepping.
</verification>

<success_criteria>
- D-M3 micro runner default on; pickArc main path bypassed
- D-M5 tap patrol wired; D-M4 done-night hold works
- pet-interrupt preserved; debug covers 11 scene keys
</success_criteria>

<output>
Create `.planning/phases/XSB-07-pet-companion/07-08-SUMMARY.md` when done
</output>
