---
phase: XSB-07-pet-companion
plan: 06
type: execute
wave: 6
depends_on: []
requirements: [COMP-01]
files_modified:
  - scripts/slice-cat-sprites.mjs
  - scripts/verify-cat-atlas.mjs
  - scripts/cat-pet-clips.manifest.json
  - miniprogram/assets/cat-pet/cat1-atlas.png
  - miniprogram/assets/cat-pet/cat1-atlas.json
  - miniprogram/assets/cat-pet/cat1-atlas-data.js
  - miniprogram/assets/cat-pet/cat1.6-atlas.png
  - miniprogram/assets/cat-pet/cat1.6-atlas.json
  - miniprogram/assets/cat-pet/cat1.9-atlas.png
  - miniprogram/assets/cat-pet/cat1.9-atlas.json
autonomous: true
must_haves:
  truths:
    - All 43 manifest clips resolve in cat1, cat1.6, and cat1.9 atlas JSON
    - cat1-atlas-data.js matches cat1-atlas.json for runtime cat-pet component
    - Source PNGs cat 1.png, cat 1.6.png, cat 1.9.png from cat-pet/ drive rebuild
    - verify-cat-atlas.mjs exits 0 for all three variants
  artifacts:
    - path: scripts/cat-pet-clips.manifest.json
      provides: 43 clip frame index definitions
    - path: miniprogram/assets/cat-pet/cat1-atlas-data.js
      provides: runtime atlas module consumed by cat-pet
    - path: scripts/verify-cat-atlas.mjs
      provides: build gate for all variants
  key_links:
    - from: scripts/slice-cat-sprites.mjs
      to: cat-pet/cat 1.png
      via: variant loop + manifest frame indices
      pattern: cat 1\.png|cat 1\.6\.png|cat 1\.9\.png
    - from: miniprogram/components/cat-pet/index.js
      to: miniprogram/assets/cat-pet/cat1-atlas-data.js
      via: require at module top
      pattern: cat1-atlas-data
---

# Plan 07-06: Three-Cat Sprite Pipeline Re-run (Wave 6)

**Goal:** Rebuild all three cat atlases from `cat-pet/` source PNGs; verify full 43-clip manifest per D-M1/D-M8 before micro-behavior runtime work.

<objective>
Wave 6 asset gate — D-M8 sprite re-run + D-M1 clip inventory lock (43 clips, no additions).

Purpose: Ensure cat1/cat1.6/cat1.9 atlases match labeled reference and manifest before pet-micro uses walk/meow/sleep clips at higher frequency.
Output: Verified atlas PNG+JSON triple + synced cat1-atlas-data.js.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/XSB-07-pet-companion/07-MICRO-CONTEXT.md
@.planning/phases/XSB-07-pet-companion/07-00-PLAN.md
@docs/PET-MICRO-BEHAVIOR.md
@cat-pet/cat 16x16 with text.png
@cat-pet/cat 1.png
@cat-pet/cat 1.6.png
@cat-pet/cat 1.9.png
@scripts/slice-cat-sprites.mjs
@scripts/verify-cat-atlas.mjs
@scripts/cat-pet-clips.manifest.json
</context>

<artifacts_this_phase_produces>
- Rebuilt `miniprogram/assets/cat-pet/cat1-atlas.{png,json}` + `cat1-atlas-data.js` from `cat-pet/cat 1.png`
- Rebuilt `miniprogram/assets/cat-pet/cat1.6-atlas.{png,json}` from `cat-pet/cat 1.6.png`
- Rebuilt `miniprogram/assets/cat-pet/cat1.9-atlas.{png,json}` from `cat-pet/cat 1.9.png`
- Verified manifest clip count = 43 keys; each clip frames[] present in all three variant JSON `frames` maps
- `npm run build:cat-atlas` green (slice + verify)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Re-run slice pipeline from cat-pet/ source PNGs (D-M8, D-M1)</name>
  <read_first>
    - scripts/slice-cat-sprites.mjs — VARIANTS array, OFFSET, OUT path, cat1-atlas-data.js emit
    - scripts/cat-pet-clips.manifest.json — 43 clip keys (idle through sad_sit_down)
    - .planning/phases/XSB-07-pet-companion/07-00-PLAN.md Task 2 — grid constants COLS=11 CELL=32 SPRITE=16
    - cat-pet/cat 16x16 with text.png — labeled section reference if frame drift suspected
  </read_first>
  <action>
    Confirm source files exist at cat-pet/cat 1.png, cat 1.6.png, cat 1.9.png (352×1696). Do NOT add new clips to manifest (D-M1: exactly existing 43).

    Run npm run build:cat-atlas (or node scripts/slice-cat-sprites.mjs directly then verify).

    If slice output shows frame misalignment vs labeled reference, tune OFFSET in slice-cat-sprites.mjs only — do not change manifest frame indices unless reference sheet proves manifest wrong; document any manifest fix in SUMMARY.

    Ensure slice script still writes cat1-atlas-data.js as module.exports copy of cat1-atlas.json (runtime require path unchanged).

    Do NOT reference cat1.6/cat1.9 in miniprogram runtime — build-only per 07-00 pattern.
  </action>
  <verify>
    <automated>npm run build:cat-atlas && test -f miniprogram/assets/cat-pet/cat1-atlas-data.js && node -e "const m=require('./scripts/cat-pet-clips.manifest.json'); if(Object.keys(m).length!==43) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - Three atlas PNG+JSON pairs exist under miniprogram/assets/cat-pet/
    - Manifest has exactly 43 clip keys
    - cat1-atlas-data.js regenerated and parseable
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Full-variant verify gate + atlas-data parity (D-M8)</name>
  <read_first>
    - scripts/verify-cat-atlas.mjs — per-variant clip frame lookup
    - miniprogram/assets/cat-pet/cat1-atlas.json vs cat1-atlas-data.js
    - miniprogram/components/cat-pet/index.js — ATLAS require path
  </read_first>
  <action>
    Run node scripts/verify-cat-atlas.mjs — must pass cat1, cat1.6, cat1.9.

    Extend verify script only if gap found: assert Object.keys(manifest) === Object.keys(atlas.clips) for each variant; every frames[] index exists in frames map with w/h matching meta.spriteSize.

    Assert cat1-atlas-data.js JSON content matches cat1-atlas.json (deep equal clips + frames keys used by manifest). If drift, re-run slice — do not hand-edit atlas-data.

    Spot-check cat1 clips used heavily by micro: walk_down, idle_a, meow_stand, sleep4_l, paw_attack_down, sad_sit_down — all resolve in verify output.

    Do NOT modify miniprogram/components/cat-pet/index.js in this plan.
  </action>
  <verify>
    <automated>node scripts/verify-cat-atlas.mjs && node -e "const a=require('./miniprogram/assets/cat-pet/cat1-atlas.json'); const d=require('./miniprogram/assets/cat-pet/cat1-atlas-data.js'); if(JSON.stringify(a.clips)!==JSON.stringify(d.clips)) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - verify-cat-atlas.mjs exit 0 for all three variants
    - cat1-atlas-data.js clips object identical to cat1-atlas.json
    - No new clips beyond manifest 43
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build script → PNG files | Reads only hardcoded cat-pet/ paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06-01 | Tampering | slice-cat-sprites.mjs | mitigate | Hardcoded source paths; no arbitrary CLI file args |
| T-07-06-02 | Denial of Service | Oversized atlas bundle | mitigate | verify-cat-atlas size sanity; 43-clip subset only |
</threat_model>

<verification>
```bash
npm run build:cat-atlas
node -e "console.log(Object.keys(require('./scripts/cat-pet-clips.manifest.json')).length)"
ls miniprogram/assets/cat-pet/cat*.png
```
</verification>

<success_criteria>
- D-M1/D-M8 satisfied: 43 clips verified across cat1, cat1.6, cat1.9
- Runtime atlas module ready for micro-behavior plans 07-07–07-08
</success_criteria>

<output>
Create `.planning/phases/XSB-07-pet-companion/07-06-SUMMARY.md` when done
</output>
