---
phase: XSB-07-pet-companion
plan: 07
type: tdd
wave: 6
depends_on: []
requirements: [COMP-01]
files_modified:
  - miniprogram/core/pet-micro.js
  - tests/core/pet-micro.test.js
  - package.json
autonomous: true
must_haves:
  truths:
    - pickNextMicroBehavior returns clip/walk/hold blocks per scene weight table
    - Hard gates block nap/settle/feast/sleep-deep on overtime-L3/L4 and hold sleep4 on done-night (D-M4)
    - onShift baseline walk weight ~40% before excited patrol (D-M2)
    - 30s/3 tap triggers 60s excited patrol with 45s cooldown on all scenes (D-M5)
    - Walk targets bias toward interest points with visitCount inverse weighting (D-M6)
    - npm run test:core includes pet-micro.test.js
  artifacts:
    - path: miniprogram/core/pet-micro.js
      provides: micro behavior pool pure functions
      exports: [createMicroSession, recordTap, isExcitedPatrol, pickNextMicroBehavior, resolveWalkTarget, onBlockComplete, sceneMicroKey]
    - path: tests/core/pet-micro.test.js
      provides: gates, patrol, interest point, Monte Carlo coverage
  key_links:
    - from: miniprogram/core/pet-micro.js
      to: miniprogram/core/pet-arcs.js
      via: sceneArcKey / resolveDoneBand reuse for scene routing keys
      pattern: sceneArcKey|resolveDoneBand
    - from: tests/core/pet-micro.test.js
      to: miniprogram/core/pet-micro.js
      via: require and deterministic rng
      pattern: pickNextMicroBehavior
---

# Plan 07-07: pet-micro.js Pure Core + Tests (Wave 6)

**Goal:** Implement micro behavior pool as testable pure module per `docs/PET-MICRO-BEHAVIOR.md` §6–8 before cat-pet runner integration.

<objective>
Wave 6 logic foundation — D-M2, D-M3 (API), D-M4, D-M5, D-M6 in isolated core with full unit tests.

Purpose: Replace arc weight semantics with block-level sampling, hard gates, excited patrol, and interest-point walk targets without UI coupling.
Output: miniprogram/core/pet-micro.js + tests/core/pet-micro.test.js wired into test:core.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/XSB-07-pet-companion/07-MICRO-CONTEXT.md
@docs/PET-MICRO-BEHAVIOR.md
@docs/PET-STATE-AND-TIME.md
@miniprogram/core/pet-arcs.js
@miniprogram/core/pet-interrupt.js
@tests/core/pet-arcs.test.js
</context>

<artifacts_this_phase_produces>
- `miniprogram/core/pet-micro.js` — constants EXCITED_TAP_WINDOW_MS=30000, EXCITED_TAP_COUNT=3, EXCITED_PATROL_MS=60000, EXCITED_COOLDOWN_MS=45000
- `createMicroSession()` → { tapTimes[], excitedPatrolUntil, excitedCooldownUntil, positionMemory, microChainRemaining }
- `recordTap(session, now)` → { triggeredPatrol: boolean } with 30s sliding window, cooldown respect
- `isExcitedPatrol(session, now)` → boolean
- `sceneMicroKey(scene, escalation, now)` — reuse pet-arcs sceneArcKey + resolveDoneBand for 11 routing keys including done-active/done-night and overtime-L1–L4
- `pickNextMicroBehavior({ scene, escalation, now, habit, session, rng, doneBand?, inSleep4Hold? })` — returns block descriptor per §2 kinds
- `resolveWalkTarget({ scene, habit, session, excited, rng, now })` — interest points windowsill/catBed/centerRug + position memory bias
- `onBlockComplete(session, block, finalPosition)` — updates visitCount, lastIdleTx/Ty
- Scene weight table from PET-MICRO-BEHAVIOR.md §6 (all 12 rows including overtime-L3/L4)
- `tests/core/pet-micro.test.js` — gates, patrol timing, interest points, Monte Carlo walk ratio
- `package.json` test:core chain includes pet-micro.test.js
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — pet-micro.test.js scaffold + hard gates + scene weights (D-M2, D-M4)</name>
  <read_first>
    - docs/PET-MICRO-BEHAVIOR.md §4 hard gates table, §6 scene weight table
    - miniprogram/core/pet-arcs.js — sceneArcKey, resolveDoneBand exports
    - tests/core/pet-arcs.test.js — assert style, habit fixture
  </read_first>
  <behavior>
    - overtime-L3 pickNextMicroBehavior never returns nap/settle/feast or sleep3/4 clips
    - overtime-L4 same gate plus hiss/sad weighted pool only
    - done-night with inSleep4Hold true returns only kind hold (no spontaneous blocks)
    - onShift baseline: Monte Carlo 500 samples walk+microWalk fraction 0.32–0.48 (D-M2 ~40%)
    - sceneMicroKey('done', 0, evening) === 'done-active'; night → 'done-night'
  </behavior>
  <action>
    Create tests/core/pet-micro.test.js with failing tests above using deterministic rng seeds.

    Create miniprogram/core/pet-micro.js stub exports matching PET-MICRO-BEHAVIOR.md §8 API signatures.

    Implement sceneMicroKey by delegating to sceneArcKey + resolveDoneBand from pet-arcs (require, do not duplicate band logic).

    Implement SCENE_WEIGHTS map from spec §6; normalize relative weights to categories: walk (walk+microWalk), idle, washYawn, meow, eat, sleepLight, sleepDeep, pawScratch, hissSad.

    Implement pickNextMicroBehavior candidate filtering per D-M4 before weighted sample.

    Map categories to concrete clips using existing 43-clip inventory only (D-M1): e.g. walk → kind walk with walkSteps 2–4; idle → idle_a/b clip; hold → sleep4_l/r for done-night terminal.

    Add package.json test:core entry: node tests/core/pet-micro.test.js after pet-arcs.test.js.

    Run tests — must fail until Task 2 completes implementation.
  </action>
  <verify>
    <automated>node tests/core/pet-micro.test.js 2>&1 | grep -q 'AssertionError\|fail\|ERR_ASSERTION' || (node tests/core/pet-micro.test.js && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists with gate and onShift weight cases
    - pet-micro.js exports API stubs
    - Tests fail RED before full implementation
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — excited patrol, interest points, chain + full test pass (D-M5, D-M6)</name>
  <read_first>
    - docs/PET-MICRO-BEHAVIOR.md §3 excited patrol, §5 interest points, §2 chain 35%
    - miniprogram/core/pet-companion.js — roamTargetRatios if reusable for napCorner tx mapping
  </read_first>
  <behavior>
    - recordTap: 3 taps within 30s sets excitedPatrolUntil = now+60000; 4th trigger blocked during cooldown 45s after patrol ends
    - isExcitedPatrol true during 60s window on any scene key including onShift and done-night (walk allowed; sleep4 hold unchanged)
    - Excited active scene Monte Carlo walk fraction 0.55–0.65 over 800 samples
    - resolveWalkTarget: catBed tx follows habit.napCorner left/center/right; visitCount inverse increases under-sampled interest id
    - pickNextMicroBehavior 35% chain: microChainRemaining 1–2 extra blocks same micro-intent
    - onBlockComplete increments visitCount for interestId targets
  </behavior>
  <action>
    Implement createMicroSession, recordTap, isExcitedPatrol with wall-clock fields on session object (mutable, session-scoped only — no storage).

    Excited patrol: walk weight ×1.5; prefer meow_* and paw_attack_*; suppress sleep/yawn when excited; resolveWalkTarget interest bias 70% when excited vs 40% baseline.

    Interest points: windowsill (0.82, 0.22), centerRug (0.50, 0.55), catBed ty 0.72 tx from napCorner; scene allow lists per spec §5 table.

    Position memory: 30% walk to lastIdle ±0.08 jitter; visitCount反比 weighting.

    Apply habit.dayMood multipliers from spec §6 (sleepy/playful/greedy) on category weights.

    Launch simplified energy window: active scenes roll 30–90s burst with 8s walk+play ×1.25 (inline in pickNextMicroBehavior, no separate energy bar).

    Implement onBlockComplete to update lastIdleTx/Ty and visitCount/lastVisitAt.

    After implementation npm run test:core must pass including pet-micro.test.js.

    Do NOT modify cat-pet/index.js in this plan.
  </action>
  <verify>
    <automated>npm run test:core</automated>
  </verify>
  <acceptance_criteria>
    - All pet-micro.test.js assertions pass
    - Exported API matches §8 signatures
    - COMP-01 behavior quality logic testable without UI
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Core pure functions | No wx/storage; session object passed from component |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07-01 | Tampering | rng injection in tests | accept | Production uses Math.random; tests use seeded fn |
| T-07-07-02 | Denial of Service | Monte Carlo test runtime | mitigate | Cap samples at 800; test completes under 5s |
</threat_model>

<verification>
```bash
npm run test:core
node tests/core/pet-micro.test.js
```
</verification>

<success_criteria>
- pet-micro.js complete per PET-MICRO-BEHAVIOR.md §8
- D-M2, D-M4, D-M5, D-M6 covered by automated tests
- Ready for 07-08 cat-pet runner integration
</success_criteria>

<output>
Create `.planning/phases/XSB-07-pet-companion/07-07-SUMMARY.md` when done
</output>
